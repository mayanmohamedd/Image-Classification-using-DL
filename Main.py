import numpy as np
import os
import Implementations as Im

IMG_SIZE = 50


train = Im.Reading_Data.create_train_data("dataset NN'23\\dataset\\train")
X_train = np.array([i[0] for i in train]).reshape(-1, IMG_SIZE, IMG_SIZE, 1)
y_train = [i[1] for i in train]

AlexNet_model = Im.AlexNet.init_AlexNet()
MODEL_NAME = 'Alex net'
Im.AlexNet.model_fit(AlexNet_model,X_train,y_train,MODEL_NAME)

#?????????????????????????????????????????????????????????????????????????????
# test = Im.Reading_Data.create_test_data("dataset NN'23\\dataset\\test")
# X_test = np.array([i[0] for i in test]).reshape(-1, IMG_SIZE, IMG_SIZE, 1)
# y_test = [i[1] for i in test]
#?????????????????????????????????????????????????????????????????????????????

